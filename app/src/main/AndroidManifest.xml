<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.punkoff.testforeveryone">

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_title_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_title_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestForEveryone">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestForEveryone.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Theme.TestForEveryone.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>